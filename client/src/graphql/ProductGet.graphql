query ProductGetProduct($id: ID!) {
  product(id: $id) {
    ... ProductListItem
    rating
    reviewCount
    colors {
      id
      name
      inStock
    }
    sizes {
      id
      type
      inStock
    }
    details {
      id
      description
    }
  }
}
