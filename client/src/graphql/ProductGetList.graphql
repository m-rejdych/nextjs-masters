query ProductGetList($first: Int, $after: ID) {
	products(first: $first, after: $after) {
		totalCount
		pageInfo {
			hasNextPage
			hasPreviousPage
		}
		edges {
			node {
				...ProductListItem
			}
		}
	}
}
